/*
public class rectangularArray {
 Assume that a two-dimensional rectangular array of integers called data has been declared with four
 rows and seven columns. Write a for loop to initialize the third row of data to store the numbers 1 through 7.
    for (int r = 2; r == 2; r++) {
        for (int c = 0; c < data[0].length; c++) {
            data[r][c] = c + 1;
        }
    }
}
*/
